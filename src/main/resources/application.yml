spring:
  application:
    name: delivery_matching

  datasource:
    url: ${DB_URL}
    username: ${DB_USERNAME}
    password: ${DB_PASSWORD}

jwt:
  secret_key: ${JWT_SECRET}

h3:
  resolution: 9
  k: 1

